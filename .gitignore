# WORM Testnet Security - Protect Sensitive Files

# Environment files with credentials
.env
*.env

# Private keys and wallet files
*.key
*.wallet

# Logs that might contain sensitive data
logs/*.log
*.log

# Backup files
*.bak
*.backup

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Node.js dependencies and build artifacts
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
yarn.lock

# React build artifacts
ui/frontend/build/
ui/frontend/.env.local
ui/frontend/.env.development.local
ui/frontend/.env.test.local
ui/frontend/.env.production.local

# Rust build artifacts
target/
Cargo.lock

# WORM specific files that might contain sensitive data
burn.json
*.burn
recovery_*.json

# Keep ABI files but exclude other JSON that might contain sensitive data
*.json
!ui/frontend/src/*.abi.json
!ui/frontend/public/manifest.json
!ui/frontend/package.json
!ui/frontend/package-lock.json
!ui/frontend/tsconfig.json
!ui/backend/package.json
!ui/backend/package-lock.json
!miner/Cargo.toml
